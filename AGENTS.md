# Agent Instructions

- Do not start Vite dev servers (`npm run dev`, `vite`, or equivalent) unless the user explicitly asks for it.
- If frontend verification would normally use a dev server, explain that a dev server is needed and wait for the user's approval before starting it.
